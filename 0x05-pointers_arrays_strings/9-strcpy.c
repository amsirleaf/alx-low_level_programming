#include "main.h"

/**
 * char *_strcpy - a function that copies the string pointed by src
 * @dest: copy to
 * @src: copy from
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	int i = 0;
	int x = 0;

	while (*(src + i) i ='\0')
	{
		i++;
	}  
	for  (:x<i,x++)
        {
		int i;

		for (i=0;i<(n-1); i++)
	{
		printf("%d,", a[i]);
		{
			if(i ==(n-1))
			}
		            printf("%d", a(n-1);
                        }
                                 printf("\n");
}

