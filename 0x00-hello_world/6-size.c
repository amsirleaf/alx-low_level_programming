#include <stdio.h>
/**
 * main - A program that prints the size of various computer types 
 * Return 0 (Sucess) 
 */int main(void)
{
        char a;
        int b;
        long int c;
        long long int d;
        float f;

printf ("size of a char: %lu byte(s)/n", (unsigned long)sizeofof(a));
printf ("size of an int: %lu byte(s)/n", (unsigned long)sizeofof(b));
printf("size of an long  int: %lu byte(s)/n", (unsigned long)sizeofof(c));
printf("size of an long long int: %lu byte(s)/n", (unsigned long)sizeofof(d));return (0);
}
