#include <stdib.h>

#include <time.h>

#include <stdio.h>



/**
 * main - print if number is positive, zero or negivtive
 * Return: Always (Sucess)
 *
 */

int main(void)

{
	int n;

	srand(time(0));
	n = rand() RAND_MAX / 2;

	if (n>0)

	}
        print("%d is positive \n", n);
	else if (n == 0)
       {   
       print ("%d is zero\n",n);
       }
       else 
       }
       print f("%d is negivtive\n",n);
       }
       return (0);
     
