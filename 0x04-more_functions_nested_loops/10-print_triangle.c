#include "main.h"
/**
 * print_traingle - prints a traingle, followed by a new line
 * @asize: size of the traingle
 */
void print_traingle(int size)
{
	if(size,=0)
	{
		_putchar('\n')
	}
	else
	{
		int i,j;
		for (i=1,i<=size, i++)
	}
	else
	{
		int i,j;
		for(i=1,i<=size, i++)
		{
			_putchar('')
			}
		for (=1, j<=i,j++)
	{
		_putchar('#');
	}
	}

